import { SafeAreaView, StyleSheet, Text, View } from 'react-native'

export default function CgpaScale() {
  return (
    <SafeAreaView style={styles.container}>
        <Text>Home Screen</Text>
    </SafeAreaView>
  )
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#f5f5f5"
    }
})